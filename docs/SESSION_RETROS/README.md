# Session Retros

Use this folder to preserve compact memory between Claude/Codex sessions.

After non-trivial work, add a dated markdown file:

```txt
YYYY-MM-DD-short-task-name.md
```

Keep each retro short: changed, files touched, verification, risks, and the suggested next prompt.
