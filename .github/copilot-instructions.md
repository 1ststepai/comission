# LeanCTX

When LeanCTX MCP tools are available, use them before native tools:

- `ctx_search` before search/grep
- `ctx_tree` before ls/find
- `ctx_read` before full file reads
- `ctx_shell` before verbose shell commands

Use native tools for edits/writes. Find the smallest relevant files before making changes.
